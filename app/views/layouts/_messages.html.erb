<% flash.each do |type, messages| %>
  <% messages.each do |msg| %>
    <div class="alert alert-<%= type == 'notice' ? "success" : "danger" %> text-center">
      <a class="close" data-dismiss="alert">&#215;</a>
      <%= content_tag :div, msg %>
    </div>
  <% end %>
<% end %>